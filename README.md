-import math
from turtle import * 
deft heart(A):
    return 15 * (math.sin(A) **3
    def heart (A):
        return 13 * math.cos(A) - 5 *\
            math.cos(2 * A) - 2 * math.cos(3 * A) - math.cos(4 * A)
            math.cos(A) - 5 *\
            math.cos(2 * A) - 2 * math.cos(3 * A)
            speed(1000)
            bgcolor("black")
            for i range(0, 628, 1):
                A = i / 100
                x = heart(A)
                y = heart(A)
                penup()
                goto(x, y)
                pendown()
                color("red")
                dot(2)
            done()
